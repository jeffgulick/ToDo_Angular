# --- Environment Variables ---
# Never commit sensitive data! Keep environment variables out of Git.
# If you need defaults, use a '.env.example' file and commit that instead.
.env
.env.*
!.env.example
!.env.template
!.env.defaults

# --- Dependencies ---
# Typically installed via 'npm install' or 'yarn install'
# Should not be version controlled.
/node_modules/
/.pnp                # Yarn Plug'n'Play
.pnp.js              # Yarn Plug'n'Play
.yarn/install-state.gz # Yarn Plug'n'Play

# --- Logs ---
# Log files generated during runtime
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
*.pid
*.seed
*.pid.lock

# --- Build Artifacts / Output ---
# Compiled files or distribution directories
# Adjust based on your build process (TypeScript, Webpack, Parcel, etc.)
dist
dist-*
build
out
lib                 # Often used for compiled TypeScript/Babel output
coverage            # Code coverage reports (e.g., from Jest, Istanbul)
.nyc_output         # NYC (Istanbul) coverage reports
*.tsbuildinfo       # TypeScript incremental build info

# --- Framework Specific ---
# Add or uncomment based on the framework you use
# Next.js
/.next/
/out/

# Nuxt.js
/.nuxt/
/dist/

# Remix
/.cache/
/build/
/public/build/

# Gatsby
/.cache/
/public/          # Uncomment if '/public' is primarily generated by Gatsby

# SvelteKit
/.svelte-kit/

# VuePress
/.vuepress/dist/

# Strapi
/build/
# .cache/ # Uncomment if needed, often contains useful cache

# --- Temporary Files / Cache ---
.tmp
*.TMP
*.swp               # Swap files (e.g., from Vim)
.eslintcache        # ESLint cache file

# --- OS Generated Files ---
# Operating System specific files (macOS, Windows)
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
._* # Resource fork files on macOS

# --- IDE / Editor Directories ---
# Ignore configuration files specific to IDEs/editors
.vscode/*
# But keep commonly shared settings (optional, adjust as needed)
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/tasks.json # Corrected duplicate
# JetBrains (WebStorm, IntelliJ IDEA, etc.)
/.idea/
# Sublime Text
*.sublime-project
*.sublime-workspace

# --- Other ---
# Optional npm cache directory (usually managed globally)
# .npm

# Optional Yarn cache directory (usually managed globally)
# .yarn-cache

# Compressed files (e.g., from 'npm pack')
*.tgz

# Optional Flow files
*.flowconfig